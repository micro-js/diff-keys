{
  "name": "@f/diff-keys",
  "description": "Take two objects and find out which keys are different",
  "repository": "git://github.com/micro-js/diff-keys.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
